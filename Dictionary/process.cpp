#include "process.h"

Process::Process()
{
}
/*
QString Process::generateRegEx(QString word)
{
    QString reg;
    for (int var = 0; var < word.length(); ++var) {
        reg.append("[a-z]?").append(word.at(var));
    }
    cout<<reg.toStdString()<<endl;
    fflush(stdout);
    return reg;
}*/
